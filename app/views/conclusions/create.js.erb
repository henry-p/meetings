<% publish_to "/meetings/#{@meeting.id}" do %>
  conclusion = ("<%= j render(@conclusion)%>")
  $('#<%= @agenda_topic.id %>'+ " .add_conclusion").replaceWith(conclusion);
<% end %>
  focusOn(conclusion);
