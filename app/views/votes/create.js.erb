<% publish_to "/meetings/#{@vote.agenda_topic.meeting.id}" do %>
  $("#<%= @vote.agenda_topic.id %> #vote").html('<%= @vote.agenda_topic.votes.count %>')
    $("#<%= @vote.agenda_topic.id %> #agenda-voter-pictures").append("<%= escape_javascript render(@vote) %>")
<% end %>